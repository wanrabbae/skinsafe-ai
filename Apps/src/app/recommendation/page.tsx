import { RecommendationView } from "@/app/components/recommendation/recommendation-view";

export const metadata = { title: "Rekomendasi produk" };

export default function RecommendationPage() {
  return <RecommendationView />;
}
